mixin search(keyword)
  form(id="form-search") 
          .input-group
            input.search(type="text", 
                  name="keyword", 
                  placeholder="Nhap noi dung",
                  class="",
                  value=keyword)
            .input-group-append 
              button(type="submit",
                class="btn btn-primary") Tim