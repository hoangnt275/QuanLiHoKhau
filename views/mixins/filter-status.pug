mixin filter-status(filterStatus)
  each item in filterStatus
          button(
            class=`btn btn-sm ml-1  btn-primary ${item.class}`
            btn-status=item.status
          ) #{item.name}