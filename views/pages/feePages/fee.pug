extends ../../layout/default.pug
include  ../../mixins/search.pug
include  ../../mixins/filter-fee-type.pug
include  ../../mixins/filter-status.pug
block main
  .container
    .card.custom-card
      .card-header
        h3 Danh sách khoản phí
      .card-body
        .controls.mb-3
          .left
            a.btn-new(href='/fee/add') + TẠO KHOẢN PHÍ MỚI
            +filter-fee-type(filterType)
          .right
            +search(keyword)
        .table-scroll
          table.table-custom
            thead
              tr
                th #
                th Mã khoản phí
                th Tên khoản phí
                th Số tiền
                th Chu kì
                th Loại phí  
                th Trạng thái
            tbody
              each val, index in fees
                tr
                  td #{index+1}
                  td #{val.codeFee}
                  td #{val.name}
                  td #{val.value}
                  td #{val.cycle}
                  td #{val.type}
                  td
                    - var cls = val.status=='Hoàn thành' ? 'status-approved' : (val.status=='Đang thu' ? 'status-blocked' : 'status-rejected')
                    span.status-dot(class=cls)
                    | #{val.status}
                  td
                    a(href='/fee/' + val.codeFee, title='Xem') 🔍
                    a.ml-2(href=`/fee/edit/${val.codeFee}`, title='Sửa') ✏️
                    form.delete-form(method="POST", action=`/fee/delete/${val.codeFee}`)
                      button.delete-button(type="submit" title='Xóa') 🗑️
        
