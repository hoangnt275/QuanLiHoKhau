extends ../../layout/default.pug
include  ../../mixins/search.pug
include  ../../mixins/filter-status.pug
block main
  .container
    .card.custom-card
      .card-header
        h3 Danh sách hộ gia đình
      .card-body
        .controls.mb-3
          .left
            a.btn-new(href='/family/add') + TẠO HỘ MỚI
            +filter-status(filterStatus)
          .right
            +search(keyword)
        table.table-custom
          thead
            tr
              th #
              th Mã hộ
              th Chủ hộ
              th Địa chỉ
              th Số thành viên
              th Trạng thái
              th Hành động
          tbody
            each val, index in families
              tr
                td #{index+1}
                td #{val.code}
                td #{val.head.fullName}
                td #{val.address}
                td #{val.members.length}
                td
                  - var cls = val.status=='Đang quản lý' ? 'status-approved' : (val.status=='Tạm khóa' ? 'status-blocked' : 'status-rejected')
                  span.status-dot(class=cls)
                  | #{val.status}
                td
                  a(href='/family/' + val.code, title='Xem') 🔍
                  a.ml-2(href='/family/edit/'+ val.code , title='Sửa') ✏️
                  form.delete-form(method="POST", action=`/family/delete/${val.code}`)
                    button.delete-button(type="submit" title='Xóa') 🗑️
        .d-flex.align-items-center.justify-content-between.mt-3
          .pagination-area
            .paginator
              a.page(href='#') ◀
              a.page.active(href='#') 1
              a.page(href='#') 2
              a.page(href='#') 3
              a.page(href='#') ▶
          .per-page
            | Hiển thị
            select
              option 10
              option 25
              option 50
              option 100
            | /trang
